using System.Windows.Forms;

namespace container_capturer.scrip_lib
{
    public static class ProcessOuput
    {
        public static void AppendTextRichTextBox(string text)
        {
            //MainPage main = new MainPage();

            //RichTextBox box = Application.OpenForms["MainPage"].Controls["RichTextBox"] as RichTextBox;

            //box.Invoke(new MethodInvoker(delegate { box.SelectionStart = box.TextLength; }));
            //box.Invoke(new MethodInvoker(delegate { box.SelectionLength = 0; }));            

            //box.Invoke(new MethodInvoker(delegate { box.AppendText(text); }));
            //box.Invoke(new MethodInvoker(delegate { box.ScrollToCaret(); }));
            //box.Invoke(new MethodInvoker(delegate { box.SelectionColor = box.ForeColor; }));
        }
    }
}
